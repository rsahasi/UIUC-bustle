/**
 * Map layer components.
 *
 * Everything drawn INSIDE a <MapView> lives here, so the map screen composes
 * markers and overlays instead of defining them inline.
 */
export { VehicleMarker, vehicleMarkerKey } from "./VehicleMarker";
export type { VehicleMarkerProps } from "./VehicleMarker";
